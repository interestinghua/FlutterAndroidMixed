<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aihuishou.mars.com.flutterandroidmixed">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OneActivity"
            android:label="@string/title_activity_one"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TwoActivity"
            android:label="@string/title_activity_two"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FlutterPluginJumpToAct"
            android:label="@string/title_activity_flutter_plugin_jump_to"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FlutterPluginCounter"
            android:label="@string/title_activity_flutter_plugin_counter"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>